# Contributing

The canonical contribution workflow lives in:
- `docs/08-governance/contributing.md`

Quick rules:
- Use short-lived branches (`feature/*`, `fix/*`, `hotfix/*`).
- Open PRs for all merges.
- Link work to `RIS-*` IDs when possible.
- Keep changes scoped and documented.
